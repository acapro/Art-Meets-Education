%h1 Kids
%table
  %thead
    %tr
      %th Name
      %th Short Description
      %th Images
      %th Content (Multi lang)
      %th{:colspan => "3"}
  %tbody
    - kids.each do |kid|
      %tr
        %td= kid.name
        %td= kid.short
        %td= kid.images.count
        - if kid.content
          %td= kid.content.truncate(40)
        - else
          %td
        %td= link_to 'Show', kid
        %td= link_to 'Edit', edit_kid_path(kid)
        %td= link_to 'Destroy', kid, method: :delete, data: { confirm: 'Are you sure?' }

= link_to 'New Kid', new_project_kid_path(@project.id)
