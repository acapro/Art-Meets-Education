%section.contact
  %aside
    .text
      %h1= t('pages.contact.title')
      %h2= t('pages.contact.description')
      
  %article
    .text
      - @contacts.each do |contact|
        - if contact.section
          = markdown(contact.section)
