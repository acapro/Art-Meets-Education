%h1 About Sections
%table
  %thead
    %tr
      %th Path
      %th Nav
      %th Content
      %th{:colspan => "3"}
  %tbody
    - @about_sections.each do |about_section|
      %tr
        %td= about_section.path
        %td= about_section.nav
        %td= about_section.content.truncate(40)
        %td= link_to 'Show', about_section
        %td= link_to 'Edit', edit_about_section_path(about_section)
        %td= link_to 'Destroy', about_section, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New About Section', new_about_section_path
