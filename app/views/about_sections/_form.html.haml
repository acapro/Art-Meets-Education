= simple_form_for(@about_section) do |f|
  = f.error_notification
  .form-inputs
    = f.input :path
    = f.input :nav, label: 'Nav text, multi language'
    = f.input :content, label: 'Content, multi language'
  .form-actions
    = f.button :submit
