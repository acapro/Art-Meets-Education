%h1 Press Page Sections
%table
  %thead
    %tr
      %th Path
      %th Nav (multi lang)
      %th Content (multi lang)
      %th{:colspan => "3"}
  %tbody
    - @press_sections.each do |press_section|
      %tr
        %td= press_section.path
        %td= press_section.nav
        - if press_section.content
          %td= press_section.content.truncate(40)
        - else
          %td
        %td= link_to 'Show', press_section
        %td= link_to 'Edit', edit_press_section_path(press_section)
        %td= link_to 'Destroy', press_section, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Press Section', new_press_section_path
